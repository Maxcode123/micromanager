# What is micromanager?

`micromanager` is a CLI application that manages multiple systems, each with
multiple projects, each with multiple compose files.

I developed it because I needed a way to centrally manage multiple systems of
microservices where each microservice had its own compose configuration. I
wanted a quick and easy way to start, stop and monitor my services.
